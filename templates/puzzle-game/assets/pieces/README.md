# Puzzle Piece Sprites

Placeholder pieces for the {{theme}} puzzle game on a {{grid_size}}x{{grid_size}} board.

Expected contents:
- `piece_0.png` through `piece_4.png` — at least 5 distinguishable piece sprites
- All pieces should share the same canvas size (e.g. 64x64 PNG with transparent margins)
